---
# tasks file for roles/front
- name: Run front
  docker_container:
    name: front
    image: raspout/my-front:latest
    networks: 
      - name: app_network
    state: started
    restart_policy: always
    pull: true